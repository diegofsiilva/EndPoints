# Arquivo de exemplo .env
# Substitua pelos seus dados reais de conexão com o PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres # Usuário padrão do Postgres, ajuste se necessário
DB_PASSWORD=senha_segura # Defina uma senha segura
DB_NAME=tarefas_db # Nome do banco de dados sugerido

# Porta para o servidor Node.js (opcional, usará 3000 se não definida)
# PORT=3000

